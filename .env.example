OPENAI_API_KEY=
# OpenAI Agents SDK model used for Everyday tasks.
TROCODE_AGENT_MODEL=gpt-5.6-luna

# Optional canary Workspace mode. Must point to the exact supported Codex CLI
# executable (currently 0.146.0). Packaged builds hide Workspace mode when the
# compatible runtime is unavailable; TroCode never rewrites the user's Codex config.
# Sign in separately with CODEX_HOME set to the app-scoped directory shown by
# TroCode's Workspace setup message.
TROCODE_CODEX_PATH=

# Hosted paid-call guard. Production should move from observe to enforce only
# after comparing ledger totals with provider billing.
TROCODE_COST_GUARD_MODE=observe
TROCODE_PAID_CALLS_ENABLED=true
TROCODE_MONTHLY_BUDGET_MICRO_USD=20000000
TROCODE_DAILY_BUDGET_MICRO_USD=2000000
TROCODE_TASK_BUDGET_MICRO_USD=500000
TROCODE_BUDGET_WARNING_PERCENT=80
TROCODE_RESERVATION_TTL_MS=120000
TROCODE_REALTIME_CALL_ESTIMATE_MICRO_USD=5000
TROCODE_SPEECH_MICRO_USD_PER_THOUSAND_CHARACTERS=60000

# Optional companion speech. These values stay in the Electron main process.
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
ELEVENLABS_MODEL_ID=eleven_flash_v2_5

GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_OAUTH_PROJECT_ID=

POSTHOG_PROJECT_TOKEN=phc_replace_with_your_project_token
POSTHOG_HOST=https://us.i.posthog.com
POSTHOG_ENVIRONMENT=development

# Local Docker PostgreSQL credentials. Keep real values in Doppler or an
# ignored environment file; never commit them here.
TROCODE_POSTGRES_PASSWORD=
DATABASE_URL=postgresql://trocode:replace_with_local_password@127.0.0.1:54320/trocode?sslmode=disable

# Public Ed25519 key used only by packaged builds without TROCODE_API_BASE_URL.
TROCODE_MEMBERSHIP_PUBLIC_KEY=

# Release-only signing and notarization inputs. Store production values in
# Doppler or GitHub Actions secrets, never in a committed environment file.
TROCODE_MACOS_SIGNING_IDENTITY=
TROCODE_APPLE_API_KEY=
TROCODE_APPLE_API_KEY_ID=
TROCODE_APPLE_API_ISSUER=
TROCODE_WINDOWS_CERTIFICATE_FILE=
TROCODE_WINDOWS_CERTIFICATE_PASSWORD=
