src/main/application/classroom-lesson-task.ts:L33: shrink: duplicated JSON request packs lesson fields already supplied in typed context. Use mode plus question/instruction for task admission; keep agent-only serialization inside the agent branch. Applied.

net: -9 lines possible.
